<!--only for the admin-->
<head>
	<title>All Blocks</title>
</head>
<h1>All blocks</h1>
<%= render 'shared/logos' %><br />

<div>
  <% if current_user %>
    <strong>Format:</strong> Block Label with actions listed below<br/><br/>
</div>
    <%= will_paginate %>
    <ul class="blocks">
      <%= render @blocks %> <! located at _block.html.erb > 
    </ul>
    <%= will_paginate %>
    <% if current_user.admin? %>
      <%= link_to "Create a Block", new_block_path %>
    <% end %>
  <% end %>