<head>
	<title>Sign Up</title>
</head>

<h1>Sign Up</h1>

<div class="row">
	<div class="span5 offset3">
		<%= form_for @user do |f| %>
			<%= render 'fields', f: f %>
			<%= f.submit "Create my account", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>